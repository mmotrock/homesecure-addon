{
  "config": {
    "step": {
      "user": {
        "title": "Connect to HomeSecure Container",
        "description": "Enter the URL of the HomeSecure container add-on. The container must be running before you continue.",
        "data": {
          "container_url": "Container URL (e.g. http://localhost:8099)",
          "api_token": "API Token (leave blank if not configured)"
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot reach the HomeSecure container. Make sure the add-on is running and the URL is correct."
    },
    "abort": {
      "already_configured": "HomeSecure is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "HomeSecure Options",
        "description": "Update the container connection settings.",
        "data": {
          "container_url": "Container URL",
          "api_token": "API Token"
        }
      }
    }
  }
}
